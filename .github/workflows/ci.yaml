name: demo pipeline
on: push
jobs:
    build:
        runs-on: ubuntu-latest
        steps:
            - name: Uptime
              run: uptime
            - name: Number Of Processors
              run: nproc


    test:
        needs: build # depends on build job (run in first) list []
        runs-on: ubuntu-latest
        steps:
            - name: Hello
              run: echo hello